# Simple Llama-2 example

This is a simple chatbot example using the pretrained Llama-2 model.

## Huggingface Token
Put your huggingface token into the file `.token`.

## Install dependencies
`pip install -r requirements.txt`
## Start
`python llama-2.py`